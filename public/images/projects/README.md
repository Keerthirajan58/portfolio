Drop real screenshots here, one folder per project slug (matches `slug` in `src/content/data.ts`). Same filenames every time:

- `cover.jpg`
- `slide-1.jpg`
- `slide-2.jpg`
- `slide-3.jpg`
- `slide-4.jpg` (optional — not every project needs 4 slides)

Folders currently waiting on real images: `siri-core-modeling`, `pitchpulse`, `face-cloaking`, `historical-event-narrator`, `coding-problem-recommender`.

`barterbrain` already has real images — copy that folder's naming pattern for the rest.
